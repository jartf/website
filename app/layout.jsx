/**
 * Minimal Root Layout
 *
 * This layout exists only to satisfy Next.js requirements.
 * Each route group ((main) and (retro)) has its own complete layout.
 */

export const metadata = {
  title: "Jarema's digital garden",
}

export default function RootLayout({ children }) {
  return children
}
