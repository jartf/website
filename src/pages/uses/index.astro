---
// Uses Page - tech and tools I use with i18n support
import BaseLayout from "@/layouts/BaseLayout.astro";
import { USES_CATEGORIES, usesDescriptions } from "@/content/uses-items";
import { t } from "@/i18n";

// Icon mapping for Lucide icons (simplified SVG paths)
const iconMap: Record<string, string> = {
  laptop: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16V7a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v9m16 0H4m16 0 1.28 2.55a1 1 0 0 1-.9 1.45H3.62a1 1 0 0 1-.9-1.45L4 16"/></svg>',
  smartphone: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="20" x="5" y="2" rx="2" ry="2"/><path d="M12 18h.01"/></svg>',
  headphones: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 14h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7a9 9 0 0 1 18 0v7a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3"/></svg>',
  globe: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20"/><path d="M2 12h20"/></svg>',
  code: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>',
  mail: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>',
  shield: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/></svg>',
  settings: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z"/><circle cx="12" cy="12" r="3"/></svg>',
  map: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.106 5.553a2 2 0 0 0 1.788 0l3.659-1.83A1 1 0 0 1 21 4.619v12.764a1 1 0 0 1-.553.894l-4.553 2.277a2 2 0 0 1-1.788 0l-4.212-2.106a2 2 0 0 0-1.788 0l-3.659 1.83A1 1 0 0 1 3 19.381V6.618a1 1 0 0 1 .553-.894l4.553-2.277a2 2 0 0 1 1.788 0z"/><path d="M15 5.764v15"/><path d="M9 3.236v15"/></svg>',
  'gamepad-2': '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="6" x2="10" y1="11" y2="11"/><line x1="8" x2="8" y1="9" y2="13"/><line x1="15" x2="15.01" y1="12" y2="12"/><line x1="18" x2="18.01" y1="10" y2="10"/><path d="M17.32 5H6.68a4 4 0 0 0-3.978 3.59c-.006.052-.01.101-.017.152C2.604 9.416 2 14.456 2 16a3 3 0 0 0 3 3c1 0 1.5-.5 2-1l1.414-1.414A2 2 0 0 1 9.828 16h4.344a2 2 0 0 1 1.414.586L17 18c.5.5 1 1 2 1a3 3 0 0 0 3-3c0-1.545-.604-6.584-.685-7.258-.007-.05-.011-.1-.017-.151A4 4 0 0 0 17.32 5z"/></svg>',
  image: '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="18" x="3" y="3" rx="2" ry="2"/><circle cx="9" cy="9" r="2"/><path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/></svg>',
  palette: '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="13.5" cy="6.5" r=".5" fill="currentColor"/><circle cx="17.5" cy="10.5" r=".5" fill="currentColor"/><circle cx="8.5" cy="7.5" r=".5" fill="currentColor"/><circle cx="6.5" cy="12.5" r=".5" fill="currentColor"/><path d="M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10c.926 0 1.648-.746 1.648-1.688 0-.437-.18-.835-.437-1.125-.29-.289-.438-.652-.438-1.125a1.64 1.64 0 0 1 1.668-1.668h1.996c3.051 0 5.555-2.503 5.555-5.555C21.965 6.012 17.461 2 12 2z"/></svg>',
  camera: '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.5 4h-5L7 7H4a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3l-2.5-3z"/><circle cx="12" cy="13" r="3"/></svg>',
  video: '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m16 13 5.223 3.482a.5.5 0 0 0 .777-.416V7.87a.5.5 0 0 0-.752-.432L16 10.5"/><rect x="2" y="6" width="14" height="12" rx="2"/></svg>',
  music: '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>',
};

// Category icons mapping
const categoryIcons: Record<string, string> = {
  hardware: "💻",
  mobile: "📱",
  audio: "🎧",
  os: "🖥️",
  development: "⌨️",
  email: "📧",
  privacy: "🔒",
  mobile_tools: "🔧",
  mapping: "🗺️",
  gaming: "🎮",
  multimedia: "🎨",
  design: "🎨",
  photo: "📷",
  video: "🎬",
  media: "🎵",
};
---

<BaseLayout title="Uses" description="The tools and tech I use daily.">
  <div class="container mx-auto px-4 py-8 lg:py-12">
    <div class="max-w-6xl mx-auto">
      <header class="mb-10 text-center">
        <h1 class="text-4xl md:text-5xl font-bold mb-4" data-i18n="uses.title">Uses</h1>
        <p class="text-muted-foreground text-lg max-w-xl mx-auto">
          <span data-i18n="uses.description">All the things I use on a daily basis. Also available on</span>
          {" "}
          <a href="https://uses.tech" target="_blank" rel="noopener noreferrer" class="text-primary hover:underline">uses.tech</a>,
          {" "}
          <span data-i18n="uses.websiteDescription">a website where people share the stuff they use.</span>
        </p>
      </header>

      <!-- Two-column layout -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12">
        {USES_CATEGORIES.map((category) => (
          <section id={category.title} class="scroll-mt-20">
            <div class="flex items-center gap-3 mb-6">
              <span class="text-2xl" aria-hidden="true">
                {categoryIcons[category.title] || "📌"}
              </span>
              <h2 class="text-2xl font-bold category-title" data-i18n={`uses.categories.${category.title}`}>
                {category.title}
              </h2>
            </div>

            {/* Main items */}
            {category.items.length > 0 && (
              <div class="space-y-4 ml-4">
                {category.items.map((item) => (
                  <div class="flex items-start gap-3">
                    <div class="flex-shrink-0 mt-1.5 w-3 h-3 rounded-full bg-primary" />
                    <div class="flex-1">
                      {item.link ? (
                        <a href={item.link} target="_blank" rel="noopener noreferrer" class="text-lg font-medium hover:text-primary flex items-center">
                          {item.name}
                          <span class="text-xs ml-1 opacity-70">↗</span>
                        </a>
                      ) : (
                        <h3 class="text-lg font-medium">{item.name}</h3>
                      )}
                      <p class="text-muted-foreground item-description" data-i18n={`uses.itemDescriptions.${item.descriptionKey}`}>
                        {t(`uses.itemDescriptions.${item.descriptionKey}`)}
                      </p>
                    </div>
                  </div>
                ))}
              </div>
            )}

            {/* Subsections */}
            {category.subsections && category.subsections.map((subsection) => (
              <div class="mt-6">
                <div class="flex items-center gap-2 mb-4">
                  <span class="text-lg" aria-hidden="true">
                    {categoryIcons[subsection.title] || "📌"}
                  </span>
                  <h3 class="text-xl font-medium subsection-title" data-i18n={`uses.subsections.${subsection.title}`}>
                    {subsection.title}
                  </h3>
                </div>
                <div class="space-y-4 ml-4">
                  {subsection.items.map((item) => (
                    item.name.startsWith('Unspeakable') ? (
                      <div
                        class="flex items-start gap-3 cursor-pointer group hidden-platform"
                        data-platform={item.name === 'Unspeakable Platform 1' ? 'revanced' : 'stremio'}
                        data-clicks="0"
                      >
                        <div class="flex-shrink-0 mt-1.5 w-3 h-3 rounded-full bg-primary" />
                        <div class="flex-1">
                          <h4 class="text-lg font-medium flex items-center gap-2">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="platform-lock">
                              <rect width="18" height="11" x="3" y="11" rx="2" ry="2"/>
                              <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
                            </svg>
                            <span class="platform-name">{item.name === 'Unspeakable Platform 1' ? 'Hidden Platform 1' : 'Hidden Platform 2'}</span>
                            <span class="text-xs text-muted-foreground platform-counter">(5 clicks to reveal)</span>
                          </h4>
                          <p class="text-muted-foreground platform-description">Click 5 times to reveal</p>
                        </div>
                      </div>
                    ) : (
                      <div class="flex items-start gap-3">
                        <div class="flex-shrink-0 mt-1.5 w-3 h-3 rounded-full bg-primary" />
                        <div class="flex-1">
                          {item.link ? (
                            <a href={item.link} target="_blank" rel="noopener noreferrer" class="text-lg font-medium hover:text-primary flex items-center">
                              {item.name}
                              <span class="text-xs ml-1 opacity-70">↗</span>
                            </a>
                          ) : (
                            <h4 class="text-lg font-medium">{item.name}</h4>
                          )}
                          {item.descriptionKey ? (
                            <p class="text-muted-foreground item-description" data-i18n={`uses.itemDescriptions.${item.descriptionKey}`}>
                              {t(`uses.itemDescriptions.${item.descriptionKey}`)}
                            </p>
                          ) : item.description && (
                            <p class="text-muted-foreground">{item.description}</p>
                          )}
                        </div>
                      </div>
                    )
                  ))}
                </div>
              </div>
            ))}
          </section>
        ))}
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  import { languageStore, t, initLanguage } from '@/i18n';

  function updateTranslations() {
    // Update all data-i18n elements (includes categories, subsections, descriptions)
    document.querySelectorAll('[data-i18n]').forEach((el) => {
      const key = el.getAttribute('data-i18n');
      if (key) {
        const translated = t(key);
        if (translated !== key) {
          el.textContent = translated;
        }
      }
    });

    // Update item descriptions using uses.itemDescriptions from JSON
    document.querySelectorAll('.item-description').forEach((el) => {
      const key = el.getAttribute('data-description-key');
      if (key) {
        const translated = t(`uses.itemDescriptions.${key}`);
        if (translated !== `uses.itemDescriptions.${key}`) {
          el.textContent = translated;
        }
      }
    });
  }

  initLanguage();
  updateTranslations();

  languageStore.subscribe(() => {
    updateTranslations();
  });

  window.addEventListener('languageChange', () => {
    updateTranslations();
  });

  // Hidden platform reveal functionality
  const platformData: Record<string, { name: string; description: string; link: string }> = {
    revanced: {
      name: 'ReVanced',
      description: 'A patcher for YouTube that removes ads and adds features',
      link: 'https://revanced.app/'
    },
    stremio: {
      name: 'Stremio',
      description: 'A media center for streaming movies and TV shows',
      link: 'https://www.stremio.com/'
    }
  };

  document.querySelectorAll('.hidden-platform').forEach((el) => {
    const platform = el.getAttribute('data-platform');
    if (!platform) return;

    el.addEventListener('click', () => {
      let clicks = parseInt(el.getAttribute('data-clicks') || '0');
      clicks++;
      el.setAttribute('data-clicks', String(clicks));

      const counter = el.querySelector('.platform-counter');
      const remaining = 5 - clicks;

      if (remaining > 0 && counter) {
        counter.textContent = `(${remaining} click${remaining === 1 ? '' : 's'} to reveal)`;
      }

      if (clicks >= 5) {
        const data = platformData[platform];
        if (data) {
          const nameEl = el.querySelector('.platform-name');
          const descEl = el.querySelector('.platform-description');
          const lockEl = el.querySelector('.platform-lock');

          if (nameEl) {
            nameEl.innerHTML = `<a href="${data.link}" target="_blank" rel="noopener noreferrer" class="hover:text-primary">${data.name} <span class="text-xs opacity-70">↗</span></a>`;
          }
          if (descEl) {
            descEl.textContent = data.description;
          }
          if (counter) {
            counter.remove();
          }
          if (lockEl) {
            // Replace lock with unlock icon
            lockEl.outerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-green-500">
              <rect width="18" height="11" x="3" y="11" rx="2" ry="2"/>
              <path d="M7 11V7a5 5 0 0 1 9.9-1"/>
            </svg>`;
          }

          el.classList.remove('cursor-pointer');
        }
      }
    });
  });
</script>
